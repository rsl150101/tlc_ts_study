// 코드 자동 완성과 가이드

function sum2(a: number, b: number): number {
  return a + b;
}

const total = sum2(10, 20);

console.log(total);
console.log(total.toString());
//- total 의 타입이 number 이여서 관련 자동완성을 지원
